# ClassQuest Builder

교실용 방탈출 학습 게임 제작 및 플레이 플랫폼.

교사가 브라우저에서 직접 방탈출 게임을 만들고, 학생들이 같은 교실에서 함께 플레이한다.

---

## 1. 프로젝트 개요

| 항목 | 내용 |
| --- | --- |
| 프로젝트명 | ClassQuest Builder |
| 대상 학습자 | 한국 초등학교 3~6학년 |
| 사용 환경 | 학교 교실 (PC실, 태블릿, 전자칠판) |
| 동시 참여 인원 | 교사 1명 + 학생 최대 30명 |
| 개발 목표 기간 | 3개월 MVP |
| 배포 형태 | 웹 애플리케이션 (설치 불필요) |

### 1.1 핵심 가치

- **교사가 코딩 없이** 자기 수업 내용으로 방탈출 게임을 만든다.
- **학생은 문제를 풀며** 방을 하나씩 탈출한다.
- **교사는 실시간으로** 학생 진행 상황을 본다.
- **만든 게임을 다른 교사와** 무료로 공유한다.

---

## 2. 대상 사용자

### 2.1 교사 (제작자 · 진행자)

- 한국 초등학교 3~6학년 담임 및 교과 전담 교사
- 코딩 지식 없음을 전제로 한다
- 수업 준비 시간이 짧다 → 제작기는 **20~30분 안에 게임 1개 완성** 가능해야 한다
- 역할: 게임 제작, 세션 개설, 진행 모니터링, 콘텐츠 공유

### 2.2 학생 (플레이어)

- 초등학교 3~6학년 (만 9~12세)
- 한 세션당 **최대 30명**
- 개인 기기 또는 모둠 공용 기기로 접속
- 별도 회원가입 없이 **세션 코드 + 이름/번호**로 참여

---

## 3. 지원 과목 및 학년

| 과목 | 3학년 | 4학년 | 5학년 | 6학년 |
| --- | :---: | :---: | :---: | :---: |
| 국어 | O | O | O | O |
| 수학 | O | O | O | O |
| 사회 | O | O | O | O |
| 과학 | O | O | O | O |
| 영어 | O | O | O | O |
| 도덕 | O | O | O | O |
| 실과 | - | - | O | O |

- 실과는 **5~6학년에서만** 선택할 수 있다.
- 과목과 학년은 게임 메타데이터이며, 콘텐츠 검색·필터의 기준이 된다.
- 과목·학년 목록은 코드에 하드코딩하지 않고 **DB 마스터 데이터**로 관리한다.

---

## 4. 게임 구조

### 4.1 구성 단위

```
게임 (Game)
 └─ 방 (Room) × 3          ← 고정 3개
     ├─ 배경 이미지
     ├─ 배치된 오브젝트 (자유 개수)
     ├─ 문제 (Question) × 3~5   ← 방마다 3~5개, 전부 필수
     └─ BGM + 환경음
```

- 하나의 게임은 **방 3개**로 구성된다.
- 각 방은 **필수 문제 3~5개**를 가진다. (게임 전체 9~15문제)
- 문제는 방 안의 **오브젝트에 연결**된다. 학생이 오브젝트를 클릭하면 문제가 열린다.
- **MVP에서는 모든 문제가 필수 문제다.**
  - 방에 있는 문제는 전부 풀어야 그 방을 나갈 수 있다.
  - **선택 문제와 보너스 문제는 지원하지 않는다.** 문제에 "필수 여부" 플래그를 두지 않는다.

### 4.2 진행 규칙

1. **자유 탐색** — 학생은 방 안의 문제 오브젝트를 **원하는 순서로** 클릭해 탐색한다. 문제 풀이 순서를 강제하지 않는다.
2. **오답 재도전** — 오답이어도 게임이 끝나지 않는다. 같은 문제를 계속 다시 풀 수 있다. **시도 횟수 제한 없음.**
3. **힌트 규칙** — **같은 학생이 같은 문제를 2회 오답**하면 그 학생에게 힌트가 열린다. 3회차 시도부터 힌트를 볼 수 있다. 힌트는 문제당 1개다.
4. **출구 개방 조건** — 현재 방의 **모든 필수 문제를 정답 처리**해야 **다음 방 출구가 열린다.**
5. **방 순서** — 방은 **1번 방 → 2번 방 → 3번 방 순차 진행**이다. 이전 방으로 되돌아갈 수는 있으나, 건너뛸 수는 없다.
6. **탈출** — 3번째 방의 문제를 모두 풀면 탈출 화면으로 전환된다.

> 정리: 자유 탐색은 **방 내부의 문제 순서**에 적용되며, **방 사이의 순서**에는 적용되지 않는다.

### 4.2.1 막힌 학생 지원

정답을 계속 맞히지 못하는 학생을 위한 경로. 진행을 건너뛰게 하지 않으면서 수업이 멈추지 않게 한다.

- **무제한 재도전**을 허용한다.
- **2회 오답 후 힌트**를 표시한다. (개인별 계산 — 5.2 참고)
- 학생 화면에 **"교사에게 도움 요청" 버튼**을 제공한다. 누르면 교사 대시보드에 요청이 표시된다.
- 교사는 대시보드에서 해당 학생의 특정 문제를 **"교사 확인 완료"**로 처리할 수 있다. 처리하면 그 학생에게 그 문제는 통과로 간주되어 출구 조건에 반영된다.
- **교사 확인 완료 기록은 정답 기록과 구분해서 저장한다.** 학습 통계와 성취 집계에서 실제 정답과 섞이지 않게 한다.
- **교사의 강제 다음 방 이동 기능은 MVP에서 제외한다.** 방을 건너뛰는 경로는 만들지 않는다.

### 4.3 문제 유형 (MVP)

| 유형 | 설명 |
| --- | --- |
| 객관식 | 보기 중 1개 선택 |
| 복수 정답 객관식 | 보기 중 여러 개 선택 |
| 단답형 | 짧은 텍스트 입력 (공백·대소문자 정규화 후 비교) |
| OX | 참/거짓 선택 |

- 각 문제는 **문제 지문, 보기/정답, 힌트 1개, 해설(선택)**을 가진다.
- **힌트는 문제당 정확히 1개**다. 다단계 힌트는 지원하지 않는다.
- 문제에 이미지를 첨부할 수 있다. (제공 자산 라이브러리에서 선택 — 10.1 참고)

---

## 5. 플레이 모드

### 5.1 개인전

- 학생 1명 = 플레이어 1명.
- 각자 자기 진행 상태(현재 방, 푼 문제, 문제별 오답 횟수, 힌트 해제 여부)를 가진다.
- 다른 학생의 진행에 영향받지 않는다.

### 5.2 기본 팀전

**팀 구성**

- 교사가 **2~6팀 중 팀 수를 선택**한다.
- 팀 정원은 따로 고정하지 않는다. **참여 학생 수를 선택한 팀 수로 균등 배정**한다. (나누어떨어지지 않으면 앞 팀부터 1명씩 더 배정)
- 교사는 자동 배정 결과를 수동으로 조정할 수 있다.

**해결 상태 — 팀 공유**

- 팀원 중 1명이 문제를 맞히면 **팀의 해당 문제는 해결 완료**로 처리한다.
- 출구 개방과 방 이동도 **팀 단위**로 일어난다.

**오답과 힌트 — 개인별**

- **오답 횟수는 학생 개인별로 계산한다.** 팀 합산이 아니다.
- **같은 학생이 같은 문제를 2회 틀리면 그 학생에게만 힌트를 표시한다.**
- **팀원에게 힌트를 자동 공유하지 않는다.** 다른 팀원은 각자 2회 오답해야 자기 힌트가 열린다.
- 이미 팀이 해결한 문제라도, 아직 안 푼 팀원은 계속 시도할 수 있고 자기 오답 횟수에 따라 힌트를 받는다.

> 요약: **해결 상태는 팀 공유, 오답·힌트는 개인 계산.**

- MVP의 팀전은 "기본" 수준이다. 팀 채팅, 역할 분담, 팀 간 경쟁 랭킹은 MVP 범위 밖이다.

### 5.3 데이터 분리 원칙

- 개인전 진행 데이터와 팀전 진행 데이터는 **서로 다른 저장 구조**를 사용한다.
- 하나의 세션은 개인전이거나 팀전이며, **중간에 모드를 바꿀 수 없다.**
- 모드 분기를 `if (isTeam)` 형태로 진행 로직 곳곳에 흩뿌리지 않는다. 진행 상태 조회·갱신은 모드별 구현으로 분리한다.
- **팀전에서도 오답·힌트는 개인 단위로 저장한다.** 팀 진행 테이블에 오답 횟수를 넣지 않는다. 시도 기록(`attempts`)은 언제나 **학생 개인** 기준이다.

---

## 6. 학생 플레이 흐름

```
세션 코드 입력
   ↓
닉네임 입력 → (팀전이면 팀 배정 확인)
   ↓
대기실  ← 교사가 시작할 때까지 대기 (대기실 전용 BGM)
   ↓
방 1  → 문제 오브젝트 자유 탐색 → 문제 풀이 → 문제 전부 정답 → 출구 개방
   ↓
방 2  → 동일
   ↓
방 3  → 동일
   ↓
탈출 화면 (전용 BGM + 효과음, 소요 시간·정답 수 표시)
```

### 6.1 학생 식별과 닉네임 중복

- 학생은 **닉네임**만 입력하고 참여한다. 회원가입·로그인은 없다.
- **닉네임 중복은 허용한다.** 같은 반에 같은 이름이 있을 수 있고, 입력을 막으면 수업이 지연된다.
- 진행 상태의 실제 키는 닉네임이 아니라 서버가 발급하는 **`studentSessionId`**다. 닉네임은 화면 표시용 라벨일 뿐이다.
- 닉네임이 겹치면 교사 대시보드에서 구분할 수 있도록 보조 정보(입장 순번 등)를 함께 표시한다.

### 6.2 재접속

- 학생이 브라우저를 닫거나 기기가 끊겨도, **`studentSessionId`로 직전 진행 상태를 그대로 복구**한다. 이 값은 브라우저에 보관하고 재입장 시 서버로 보낸다.
- 토큰이 없어진 경우(다른 기기, 저장소 삭제)에는 세션 코드로 재입장한 뒤 **교사가 대시보드에서 기존 참여자와 연결**해 준다. 닉네임만으로 자동 매칭하지 않는다. (중복 닉네임을 잘못 이어붙일 수 있다)
- 복구 대상: 현재 방, 푼 문제 목록, **개인별** 문제별 오답 횟수, 힌트 해제 여부, 팀 소속, 교사 확인 완료 기록.
- 진행 상태의 원본은 **항상 서버(DB)**다. 브라우저 저장소는 `studentSessionId` 보관과 캐시 용도로만 쓴다.

---

## 7. 교사용 게임 제작기

### 7.1 편집 방식

- **자유 드래그 배치**. 격자에 스냅되지 않으며, 오브젝트를 방 배경 위 어디에나 놓을 수 있다.
- 캔버스 좌표는 **정규화 좌표(0~1)**로 저장한다. 화면 크기가 달라도 배치가 유지된다.

### 7.2 오브젝트 편집 기능

| 기능 | 설명 |
| --- | --- |
| 이동 | 드래그로 위치 변경 |
| 크기 조절 | 핸들 드래그, 비율 고정 옵션 |
| 회전 | 회전 핸들 (각도 단위 조절) |
| 복사 | 오브젝트 복제 (문제 연결 포함 여부 선택) |
| 삭제 | 선택 오브젝트 제거 |
| 잠금 | 잠긴 오브젝트는 선택·이동되지 않음 (배경 장식용) |
| 레이어 | 앞으로/뒤로 보내기, z-순서 관리 |
| Undo / Redo | 편집 이력 되돌리기·다시 실행 |

### 7.3 자유 배치 안전 규칙

자유 배치를 허용하되, 학생이 플레이할 수 없는 게임이 만들어지는 것은 막는다.

- **화면 이탈 방지** — 오브젝트는 방 영역을 완전히 벗어날 수 없다. 최소 일부가 항상 보이도록 좌표를 클램프한다.
- **최소 클릭 크기** — 문제가 연결된 오브젝트는 지정된 최소 크기(터치 기준) 아래로 줄일 수 없다.
- **완전 가림 경고** — 문제가 연결된 오브젝트가 다른 오브젝트에 완전히 가려지면 저장 시 경고한다.
- **겹침 허용** — 겹침 자체는 허용한다. 자유로운 연출을 막지 않는다.
- **게시 전 검증** — 게시 시점에 다음을 검사하고, 통과하지 못하면 게시를 막는다.
  - 방이 정확히 3개인가
  - 방마다 문제가 3~5개인가 (모든 문제가 필수이므로 별도 필수 개수 검사는 없다)
  - 모든 문제가 클릭 가능한 오브젝트에 연결되어 있는가
  - 모든 문제에 정답과 **힌트 1개**가 있는가
  - 방마다 BGM과 환경음이 지정되어 있는가

### 7.4 자동 저장과 버전 관리

게임은 **초안(draft)**과 **게시 버전(published version)**으로 나뉜다.

- **자동 저장은 작성 중인 초안에만 적용한다.** (디바운스 저장 + 저장 상태 표시) 게시 버전에는 자동 저장이 동작하지 않는다.
- **게시 시 불변 스냅샷 버전을 생성한다.** 게시 시점의 방·오브젝트·문제·정답·힌트·오디오 구성을 통째로 복사해 보관한다.
- **게시된 버전은 직접 수정하지 않는다.** 게시본을 UPDATE하거나 DELETE하는 경로를 만들지 않는다.
- **게시 후 수정하려면 새로운 초안을 만들거나 기존 초안을 편집한 뒤 새 버전으로 다시 게시한다.**
- **진행 중인 수업은 시작 당시의 게시 버전을 계속 사용한다.** 교사가 게임을 수정하고 새 버전을 게시해도, 이미 진행 중인 세션의 내용은 바뀌지 않는다.
- 교사는 이전 게시 버전 목록을 보고, 특정 버전의 내용을 **초안으로 불러올 수** 있다. (게시 버전 자체는 그대로 남는다)

---

## 8. 실시간 학생 진행 확인

교사 대시보드에서 세션 진행 상황을 실시간으로 본다.

- 접속한 학생/팀 목록과 온라인 여부 (닉네임 중복 시 입장 순번 등으로 구분)
- 학생(팀)별 **현재 방**
- 학생(팀)별 **푼 문제 수 / 전체 문제 수**
- **오답이 많은 문제** 집계 → 어떤 문제에서 막히는지 파악
- 힌트를 연 학생 수
- **도움 요청 알림** — "교사에게 도움 요청" 버튼을 누른 학생과 해당 문제
- 탈출 완료자 목록과 소요 시간

교사 조작:
- 세션 시작 / 일시정지 / 종료
- **특정 학생의 특정 문제를 "교사 확인 완료"로 처리** — 그 문제를 통과로 인정한다. 정답 기록과 분리해 저장한다.
- 특정 학생 재입장 허용 및 기존 참여자와 연결
- 세션 시간 제한을 켠 경우 남은 시간 표시

MVP 제외: **다음 방으로 강제 이동시키는 기능은 넣지 않는다.** 진행 보조는 "교사 확인 완료"로만 한다.

기술: **Supabase Realtime** 구독으로 진행 상태 변화를 push 받는다. 폴링을 기본 수단으로 쓰지 않는다.

### 8.1 세션 시간 제한

- 시간 제한은 **선택 기능**이다. **기본값은 제한 없음.**
- 교사가 세션을 만들 때 제한 시간을 켜고 값을 지정할 수 있다.
- 시간이 끝나면 세션이 종료되고 결과 화면으로 넘어간다. 남은 문제는 미해결로 기록한다.
- 남은 시간 계산의 기준은 **서버 시각**이다. 클라이언트 타이머는 표시용이다.

---

## 9. 콘텐츠 공유

- 교사는 자기가 만든 게임을 **공개(public)**로 전환할 수 있다.
- 다른 교사는 공개된 게임을 **검색**한다. (과목, 학년, 키워드, 최신순/인기순)
- **미리보기** — 플레이 화면을 직접 확인한다. **미리보기에서도 학생 플레이와 완전히 동일한 서버 판정 방식을 사용한다.** 미리보기 전용 채점 경로나 정답 노출 예외를 만들지 않는다.
- **복제** — 공개 게임을 자기 계정으로 복사한다. 복사본은 **새로운 초안**이 되며 자유롭게 수정할 수 있다. 원본은 영향받지 않는다.
- **유료 콘텐츠 거래는 범위에서 제외한다.** 결제, 정산, 가격 책정, 크레딧 기능은 만들지 않는다. **무료 공개·검색·미리보기·복제만 지원한다.**

---

## 10. 오디오 설계

| 구간 | 오디오 |
| --- | --- |
| 대기실 | **대기실 전용 BGM** + 대기실 효과음 (입장, 준비 완료 등) |
| 방 1 | 방 1 전용 BGM + 방 1 환경음 |
| 방 2 | 방 2 전용 BGM + 방 2 환경음 |
| 방 3 | 방 3 전용 BGM + 방 3 환경음 |
| 탈출 화면 | **탈출 전용 BGM** + 탈출 효과음 (성공 팡파르 등) |

공통 효과음: 정답, 오답, 힌트 열림, 오브젝트 클릭, 방 이동.

원칙:
- **방마다 서로 다른 BGM과 환경음**을 설정할 수 있다. 교사가 오디오 라이브러리에서 고른다.
- BGM은 루프 재생, 환경음은 루프 재생, 효과음은 1회 재생.
- 오디오 파일 경로·목록을 코드에 하드코딩하지 않는다. **DB의 오디오 에셋 테이블**에서 조회한다.
- 학생·교사 모두 **전체 음소거 / BGM만 음소거**를 켤 수 있다. 교실 환경에서 소리를 못 쓰는 경우를 고려한다.
- 브라우저 자동재생 정책 때문에, BGM은 **최초 사용자 상호작용 이후** 시작한다.

### 10.1 자산 등록 정책

- **효과음과 BGM 파일은 관리자 또는 개발자가 등록한 자산에서만 선택한다.**
- **교사의 사용자 오브젝트 이미지 업로드는 MVP에서 제외한다.** 배경·오브젝트·캐릭터 이미지도 제공 라이브러리에서만 고른다.
- 따라서 MVP에는 **교사용 파일 업로드 기능이 없다.** 업로드 UI, 스토리지 버킷 쓰기 권한, 파일 검증·용량 제한 로직을 만들지 않는다.
- 자산은 `assets/` 아래에 두고, **라이선스가 확인된 파일만** 등록한다. 자산 목록은 시드 스크립트로 DB에 넣는다.

---

## 11. 범위에서 제외하는 것

MVP 범위를 지키기 위해 아래는 **만들지 않는다.**

- **AI 기능 전체** — 문제 자동 생성, 자동 채점 보조, 힌트 자동 생성, 이미지 생성, 챗봇 튜터, 추천 알고리즘 등 **모든 AI/LLM 연동을 제외**한다.
- **유료 콘텐츠 거래** — 결제, 마켓플레이스 판매, 구독, 크레딧.
- **선택 문제·보너스 문제** — MVP의 모든 문제는 필수다.
- **교사의 강제 다음 방 이동** — 진행 보조는 "교사 확인 완료"로만 한다.
- **교사의 사용자 이미지·오디오 업로드** — 제공 자산 라이브러리에서만 선택한다.
- **다단계 힌트** — 문제당 힌트는 1개다.
- 학부모 계정, 학교/교육청 관리자 계정.
- 학생 개인 회원가입 및 로그인.
- 방 개수 가변화 (MVP는 3개 고정).
- 팀 간 실시간 경쟁 랭킹, 팀 채팅.
- 모바일 네이티브 앱.
- 오프라인 플레이.
- 외부 LMS 연동.

---

## 12. 기술 스택

| 영역 | 기술 |
| --- | --- |
| 프레임워크 | **Next.js** (App Router) |
| UI 라이브러리 | **React** |
| 언어 | **TypeScript** |
| 스타일링 | **Tailwind CSS** |
| 백엔드 · 인증 | **Supabase** |
| 데이터베이스 | **PostgreSQL** (Supabase 관리형) |
| 실시간 통신 | **Supabase Realtime** |
| 배포 | **Vercel** |
| 단위·통합 테스트 | **Vitest** |
| E2E 테스트 | **Playwright** |

### 12.1 보안 원칙

- **정답 원문을 브라우저 번들, HTML, 클라이언트 상태에 포함하지 않는다.** 문제 조회 API는 지문·보기만 반환하고 정답 필드를 제외한다. 서버 컴포넌트의 직렬화 payload, `data-*` 속성, 소스맵에도 들어가지 않게 한다.
- **정답 판정은 서버에서 한다.** 학생 답안을 서버로 보내 채점하고 결과만 돌려준다.
- **미리보기도 학생 플레이와 동일한 서버 판정 경로를 쓴다.** 미리보기 전용 예외를 만들지 않는다.
- **정답을 볼 수 있는 곳은 교사용 문제 편집 화면뿐이다.** 그 화면에서도 서버가 **해당 게임의 소유 교사인지 권한을 확인한 뒤에만** 정답을 반환한다.
- **힌트도 서버에서 잠금 해제한다.** 해당 학생의 오답 2회 조건을 서버가 검증한 뒤에만 힌트 본문을 반환한다.
- **Supabase RLS(Row Level Security)를 모든 테이블에 적용**한다.
  - 교사는 자기 게임만 수정한다.
  - 학생은 자기(또는 자기 팀) 진행 상태만 읽고 쓴다.
  - 정답·힌트 원본 테이블은 학생 역할에서 직접 접근할 수 없다.
- 진행 상태는 **서버가 단일 원본**이다. 클라이언트가 보낸 "이 방 다 풀었어요"를 신뢰하지 않는다.

---

## 13. 데이터 모델 개요

MVP에서 다룰 주요 엔티티. (상세 스키마는 `docs/` 및 `supabase/` 마이그레이션에서 정의한다.)

| 엔티티 | 설명 |
| --- | --- |
| `teachers` | 교사 계정 |
| `games` | 게임 메타데이터 (제목, 과목, 학년, 공개 여부) |
| `game_versions` | 게시 버전 스냅샷 (불변) |
| `rooms` | 방 (게임당 3개, 순서, 배경, BGM, 환경음) |
| `objects` | 방에 배치된 오브젝트 (좌표, 크기, 회전, z-순서, 잠금) |
| `questions` | 문제 (유형, 지문, 보기, 연결 오브젝트) — **필수 여부 컬럼 없음. 전부 필수다.** |
| `question_answers` | 정답·힌트(1개)·해설 (학생 역할 접근 차단) |
| `audio_assets` | BGM·환경음·효과음 라이브러리 (관리자·개발자만 등록) |
| `image_assets` | 배경·오브젝트·캐릭터 이미지 라이브러리 (관리자·개발자만 등록) |
| `sessions` | 수업 세션 (세션 코드, 모드, 고정된 게임 버전, 팀 수, 시간 제한) |
| `participants` | 참여 학생 (`studentSessionId`, 닉네임, 입장 순번) — **닉네임 중복 허용** |
| `teams` / `team_members` | 팀전 팀 구성 (팀 2~6개) |
| `player_progress` | **개인전** 진행 상태 |
| `team_progress` | **팀전** 진행 상태 (해결한 문제, 현재 방) — 오답 횟수를 두지 않는다 |
| `attempts` | 문제 시도 기록 (정오답, 시각) — **항상 학생 개인 기준.** 오답 통계와 힌트 해제 판정의 근거 |
| `hint_unlocks` | 학생별 힌트 해제 기록 — **개인 단위.** 팀에 공유하지 않는다 |
| `help_requests` | 학생의 "교사에게 도움 요청" 기록 (학생, 문제, 시각, 처리 여부) |
| `teacher_overrides` | **"교사 확인 완료"** 기록 (학생, 문제, 처리 교사, 시각) — **정답 기록(`attempts`)과 분리 저장** |

---

## 14. 3개월 MVP 일정

| 기간 | 목표 |
| --- | --- |
| **1개월차** | 프로젝트 기반 구축. Supabase 스키마 + RLS, 교사 인증, 게임/방/문제 CRUD, 제작기 캔버스 기본 배치(이동·크기·삭제) |
| **2개월차** | 제작기 완성(회전·복사·잠금·레이어·Undo/Redo·초안 자동 저장·게시 스냅샷/버전), 학생 플레이 화면, 서버 채점, 개인별 힌트 해제, 출구 개방·방 이동 |
| **3개월차** | 개인전/팀전 세션(팀 균등 배정), 실시간 교사 대시보드, 도움 요청·교사 확인 완료, 재접속 복구, 오디오, 콘텐츠 공개·검색·미리보기·복제, 테스트 보강, Vercel 배포 |

### 14.1 MVP 완료 기준

- 교사가 로그인해서 게임 1개(방 3개, 문제 9~15개)를 만들어 게시할 수 있다.
- 게시 후 초안을 수정해도 이미 게시한 버전과 진행 중인 세션이 바뀌지 않는다.
- 학생 30명이 세션에 접속해 개인전과 팀전을 각각 끝까지 플레이할 수 있다.
- 팀전에서 해결 상태는 팀에 공유되고, 오답·힌트는 개인별로 동작한다.
- 정답이 브라우저 네트워크 응답·페이지 HTML·번들에 노출되지 않는다. 미리보기도 마찬가지다.
- 막힌 학생이 도움을 요청하고, 교사가 "교사 확인 완료"로 처리해 진행할 수 있다. 그 기록은 정답과 분리 저장된다.
- 학생이 새로고침·재접속해도 진행 상태가 복구되며, 닉네임이 중복돼도 섞이지 않는다.
- 교사 대시보드에서 진행 상황이 실시간으로 갱신된다.
- 핵심 로직에 Vitest 테스트, 주요 흐름에 Playwright 테스트가 있다.

---

## 15. 확정된 세부 결정

2026-08-02 확정. 결정 배경과 추후 검토 항목은 [docs/01_REQUIREMENTS_DECISIONS.md](docs/01_REQUIREMENTS_DECISIONS.md)에 기록한다.

| 항목 | 확정 내용 |
| --- | --- |
| 문제 필수/선택 | **모든 문제가 필수.** 선택·보너스 문제 없음. 방마다 3~5개 |
| 방 진행 | 방 안은 자유 탐색, 방 사이는 1 → 2 → 3 순차. 현재 방 전부 정답 시 출구 개방 |
| 팀 수 | 교사가 **2~6팀 중 선택** |
| 팀 정원 | 고정하지 않음. 학생 수를 팀 수로 **균등 배정** |
| 팀전 힌트 | **오답 횟수는 개인별.** 본인 2회 오답 시 본인에게만 힌트. 팀 자동 공유 없음 |
| 팀전 해결 상태 | 팀원 1명이 풀면 **팀의 해당 문제는 해결 완료** |
| 세션 시간 제한 | **선택 기능, 기본값 제한 없음** |
| 닉네임 중복 | **허용.** 내부 `studentSessionId`로 구분 |
| 힌트 개수 | 문제당 **1개** |
| 자산 등록 | BGM·효과음·이미지 모두 **관리자 또는 개발자가 등록한 자산에서만 선택** |
| 교사 이미지 업로드 | **MVP 제외** |
| 막힌 학생 지원 | 무제한 재도전 + 2회 오답 힌트 + 도움 요청 버튼 + **교사 확인 완료** (정답과 분리 기록) |
| 강제 방 이동 | **MVP 제외** |
| 유료 거래 | **제외.** 무료 공개·검색·미리보기·복제만 지원 |
| 자동 저장 | **초안에만 적용.** 게시 시 불변 스냅샷 생성, 진행 중 수업은 시작 당시 버전 유지 |
| 정답 보안 | 번들·HTML·클라이언트 상태에 정답 원문 없음. 교사용 편집 화면에서만 권한 확인 후 조회 |


---

## 부록: 기준 문서

- [docs/00_PROJECT_WORKFLOW.md](docs/00_PROJECT_WORKFLOW.md) — 역할·승인·문서 충돌 해결
- [docs/01_REQUIREMENTS_DECISIONS.md](docs/01_REQUIREMENTS_DECISIONS.md) — 확정 결정과 이유
- [docs/sprints/Sprint-01-Setup-Plan.md](docs/sprints/Sprint-01-Setup-Plan.md) — 현재 승인 대기 중인 셋업 계획
- [docs/02_ARCHITECTURE.md](docs/02_ARCHITECTURE.md) — 시스템 경계와 구현 구조

사양 변경은 이 문서와 요구사항 결정 기록을 먼저 갱신한 뒤 구현한다.


## 운영 문서

- [tasks/NEXT_TASK.md](tasks/NEXT_TASK.md) — 현재 Sprint의 실행 항목
- [tasks/BACKLOG.md](tasks/BACKLOG.md) — 승인 전·후순위 작업
- [tasks/DONE.md](tasks/DONE.md) — 완료된 주요 작업 요약
- [changelog/README.md](changelog/README.md) — Sprint별 변경 이력 목차
- [decisions/README.md](decisions/README.md) — 주요 아키텍처 결정 기록(ADR) 목차
