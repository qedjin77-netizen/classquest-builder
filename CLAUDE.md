# CLAUDE.md — ClassQuest Builder 개발 규칙

이 문서는 Claude Code가 이 저장소에서 작업할 때 **항상 따라야 하는 규칙**이다.
사용자의 명시적 지시가 이 문서와 충돌하면 사용자의 지시를 따르되, 충돌 사실을 먼저 알린다.

---

## 1. 작업 시작 전

### 1.1 문서를 먼저 읽는다

코드를 쓰거나 파일을 고치기 전에 반드시 읽는다.

1. `PROJECT.md` — 프로젝트 전체 사양. 항상 읽는다.
2. `docs/00_REQUIREMENTS_DECISIONS.md` — 확정된 요구사항 결정과 그 이유. **항상 읽는다.**
3. `docs/` 안의 나머지 관련 문서 — 지금 건드리는 영역에 해당하는 것.
4. 수정 대상 파일과 그 파일이 의존하는 파일.

확정된 결정을 다시 논의하거나 뒤집지 않는다. 바꿔야 할 이유가 생기면 **먼저 알리고 승인을 받은 뒤** 결정 문서부터 갱신한다.

읽지 않은 상태에서 사양을 추측하지 않는다. `PROJECT.md`에 답이 없으면 **추측하지 말고 질문한다.**

### 1.2 사양과 다르면 멈춘다

작업 요청이 `PROJECT.md`와 어긋나면, 코드를 쓰기 전에 그 사실을 알린다.
문서를 조용히 무시하지도, 코드를 조용히 문서에 맞추지도 않는다.

---

## 2. 작업 방식

### 2.1 큰 작업은 작은 단계로 나눈다

- 하나의 작업 단위는 **파일 몇 개, 기능 하나** 수준으로 유지한다.
- 시작하기 전에 단계 목록을 만들어 보여준다.
- 한 번에 수십 개 파일을 생성하거나, 여러 기능을 한꺼번에 구현하지 않는다.
- 각 단계가 끝나면 그 단계의 결과를 보고하고 다음으로 넘어간다.

### 2.2 요구사항을 임의로 확대하지 않는다

- **요청받은 것만 만든다.**
- "이것도 있으면 좋을 것 같아서" 추가하지 않는다.
- 필요해 보이는 기능이 있으면 **제안만 하고 구현하지 않는다.**
- `PROJECT.md`의 "범위에서 제외하는 것" 목록에 있는 기능은 만들지 않는다.
- 리팩터링도 마찬가지다. 요청받지 않은 파일을 정리하거나 구조를 바꾸지 않는다.

### 2.3 라이브러리를 함부로 늘리지 않는다

- `PROJECT.md`의 기술 스택에 없는 의존성을 추가하려면 **먼저 이유를 설명하고 승인을 받는다.**
- 기존 스택으로 해결되는 문제에 새 라이브러리를 넣지 않는다.

---

## 3. 절대 금지 규칙

### 3.1 AI 기능 구현 금지

이 프로젝트에는 **어떤 AI/LLM 기능도 넣지 않는다.**

금지 대상:
- 문제 자동 생성, 보기 자동 생성, 힌트 자동 생성, 해설 자동 생성
- 서술형 자동 채점, 유사 답안 판정
- 챗봇, 튜터, 학습 추천, 난이도 자동 조절
- 이미지·오디오 생성
- OpenAI / Anthropic / Google 등 **모든 LLM API 클라이언트 및 SDK 의존성**

사용자가 AI 기능을 요청하면, 구현하기 전에 이 규칙을 알리고 확인을 받는다.

### 3.2 정답을 클라이언트에 노출하지 않는다

- 학생용 문제 조회 응답에 **정답, 정답 인덱스, 해설, 잠기지 않은 힌트 본문을 포함하지 않는다.**
- `select('*')`로 문제 테이블을 조회하지 않는다. **필요한 컬럼만 명시**한다.
- **정답 원문을 브라우저 번들, HTML, 클라이언트 상태에 넣지 않는다.** 클라이언트 상태, props, `data-*` 속성, 번들 상수, localStorage, 서버 컴포넌트 직렬화 payload 전부 해당한다.
- **미리보기도 학생 플레이와 동일한 서버 판정 경로를 쓴다.** 미리보기 전용 API나 정답 노출 예외를 만들지 않는다.
- **정답을 반환해도 되는 곳은 교사용 문제 편집 화면뿐이다.** 그 경로에서도 서버가 **요청자가 해당 게임의 소유 교사인지 확인한 뒤에만** 정답을 반환한다. 권한 확인 없는 정답 조회 엔드포인트를 만들지 않는다.
- 정답·힌트는 별도 테이블(`question_answers`)에 두고, 학생 역할이 접근할 수 없게 한다.

### 3.3 정답 판정은 서버에서 한다

- 채점 로직은 **서버(Route Handler / Server Action / Postgres 함수)에만** 존재한다.
- 클라이언트는 답안을 보내고 `{ correct: boolean, ... }` 결과만 받는다.
- 채점 함수를 클라이언트와 공유되는 모듈에 두지 않는다.
- 진행 상태(푼 문제, 출구 개방, 방 이동, 탈출)는 **서버가 판단하고 서버가 기록**한다.
  클라이언트가 보낸 "다 풀었어요", "다음 방으로 갈게요"를 신뢰하지 않는다.
- 힌트 해제도 서버가 판정한다. 서버가 **해당 학생의** 오답 2회를 확인한 뒤에만 힌트 본문을 반환한다.
- 세션 시간 제한을 쓸 때 남은 시간 판정 기준은 **서버 시각**이다. 클라이언트 타이머는 표시용이다.

### 3.4 하드코딩 금지

**방, 오브젝트, 문제, 오디오를 코드에 하드코딩하지 않는다.**

- 방 배경, 오브젝트 이미지 목록, 문제 내용, BGM·환경음·효과음 파일 경로는 **모두 DB에서 조회**한다.
- 샘플/데모 데이터와 **관리자·개발자가 등록하는 자산 목록**은 코드가 아니라 **시드 스크립트(`supabase/seed`)**에 넣는다.
- 과목·학년 목록도 마스터 데이터로 관리한다.
- 예외: 방 개수 3개, 방당 문제 3~5개, 힌트 해제 오답 2회, 팀 수 2~6 같은 **규칙 상수**는 코드에 둘 수 있다. 단 한 곳(`constants`)에 모으고 여러 파일에 흩뿌리지 않는다.

---

## 4. 도메인 규칙

### 4.1 Supabase RLS를 반드시 적용한다

- **새로 만드는 모든 테이블에 RLS를 켠다.** RLS 없는 테이블을 만들지 않는다.
- 테이블 생성 마이그레이션과 정책 마이그레이션을 같이 작성한다.
- 정책 원칙:
  - 교사는 **자기 게임**만 읽고 쓴다.
  - 공개된 게임은 다른 교사가 **읽기만** 할 수 있다.
  - 학생(익명 참여자)은 **자기 또는 자기 팀의 진행 상태**만 읽고 쓴다.
  - 정답·힌트 테이블은 학생 역할에서 **직접 접근 불가**.
- `service_role` 키를 클라이언트 코드나 `NEXT_PUBLIC_*` 환경변수에 넣지 않는다.
- RLS를 우회하기 위해 서버에서 무조건 `service_role`을 쓰지 않는다. 꼭 필요한 지점에서만 쓰고 이유를 남긴다.

### 4.2 개인전과 팀전 데이터를 분리한다

- 진행 상태는 `player_progress`(개인전)와 `team_progress`(팀전)로 **테이블을 나눈다.** 한 테이블에 nullable 컬럼으로 섞지 않는다.
- 진행 상태 조회·갱신은 모드별 구현으로 분리한다. 진행 로직 전반에 `if (isTeam)` 분기를 흩뿌리지 않는다.
- 세션의 모드는 **생성 시 고정**이며 중간에 바뀌지 않는다.
- 개인전 코드에서 팀 테이블을, 팀전 코드에서 개인 진행 테이블을 참조하지 않는다.

**팀전 규칙 — 해결 상태는 팀 공유, 오답·힌트는 개인 계산**

- 팀원 1명이 문제를 맞히면 **팀의 해당 문제는 해결 완료**로 처리한다. 출구 개방과 방 이동도 팀 단위다.
- **오답 횟수는 학생 개인별로 센다.** 팀 합산으로 세지 않는다.
- **같은 학생이 같은 문제를 2회 틀렸을 때 그 학생에게만 힌트를 준다.** 팀원에게 자동 공유하지 않는다.
- 따라서 `attempts`와 `hint_unlocks`는 **팀전에서도 학생 개인 기준**으로 기록한다. `team_progress`에 오답 횟수 컬럼을 만들지 않는다.
- 팀이 이미 해결한 문제라도 아직 안 푼 팀원은 계속 시도할 수 있어야 하고, 자기 오답 횟수에 따라 힌트를 받는다.
- 팀 수는 **2~6팀**이며 교사가 고른다. 학생은 팀 수에 맞춰 **균등 배정**한다. 팀 정원 상수를 만들지 않는다.

### 4.3 진행 규칙을 임의로 바꾸지 않는다

- **모든 문제는 필수다.** `questions`에 `is_required` 같은 필수 여부 컬럼을 만들지 않는다. 선택 문제·보너스 문제 개념을 도입하지 않는다.
- **현재 방의 모든 문제를 정답 처리해야 다음 방 출구가 열린다.** 방은 1 → 2 → 3 순차 진행이며 건너뛸 수 없다.
- 방 내부에서는 **문제 풀이 순서를 강제하지 않는다.**
- **재도전은 무제한**이다. 시도 횟수 제한을 넣지 않는다.
- **힌트는 문제당 1개**다. 다단계 힌트 구조를 만들지 않는다.
- **교사의 강제 다음 방 이동 기능은 만들지 않는다.** 막힌 학생 지원은 "도움 요청 → 교사 확인 완료" 경로로만 구현한다.
- **"교사 확인 완료"(`teacher_overrides`)는 정답 기록(`attempts`)과 분리해서 저장한다.** 교사 확인을 정답으로 기록하거나 정답 통계에 합산하지 않는다. 출구 조건 판정에는 반영하되, 학습 성취 집계에서는 구분해서 다룬다.
- 세션 시간 제한은 **선택 기능이고 기본값은 제한 없음**이다. 항상 켜져 있다고 가정하지 않는다.

### 4.4 자유 배치 안전 규칙을 지킨다

자유 드래그 배치를 구현할 때 다음을 항상 함께 구현한다.

- 오브젝트 좌표는 **정규화(0~1)로 저장**한다. 픽셀 좌표를 그대로 저장하지 않는다.
- 오브젝트가 방 영역을 **완전히 벗어나지 못하게** 좌표를 클램프한다.
- 문제가 연결된 오브젝트는 **최소 클릭 크기** 아래로 줄일 수 없다.
- 문제가 연결된 오브젝트가 완전히 가려지면 경고한다.
- 겹침 자체는 막지 않는다. (연출 자유 허용)
- **게시 검증**을 통과하지 못하면 게시를 막는다: 방 3개, 방당 문제 3~5개, 모든 문제의 오브젝트 연결, 모든 문제의 정답·힌트 1개 존재, 방마다 BGM·환경음 지정.

### 4.5 게시된 게임 버전은 불변이다

- **자동 저장은 초안에만 적용한다.** 게시 버전에 자동 저장이 닿는 경로를 만들지 않는다.
- **게시 시 불변 스냅샷 버전을 생성한다.** 참조만 걸어두고 원본 초안을 계속 가리키게 하지 않는다. 초안을 고치면 과거 게시본이 함께 바뀌는 구조는 잘못된 구현이다.
- 게시된 버전(`game_versions`)의 방·오브젝트·문제·정답·오디오를 **수정하거나 삭제하지 않는다.** 게시본을 직접 UPDATE/DELETE하는 코드를 쓰지 않는다.
- 게시 후 수정은 항상 **새 초안 또는 기존 초안 편집 → 새 버전 게시** 경로로만 이뤄진다.
- **진행 중인 수업은 시작 당시의 게시 버전을 계속 사용한다.** 세션은 버전 ID를 고정해서 참조하며, "최신 버전"을 조회하지 않는다.
- 게임 복제는 **새 초안**을 만든다. 원본을 건드리지 않는다.
- 마이그레이션으로 과거 게시 버전의 콘텐츠를 변경하지 않는다.

### 4.6 학생 식별과 자산 정책

- **닉네임 중복을 막지 않는다.** 진행 상태의 키는 언제나 서버가 발급한 `studentSessionId`이며, 닉네임은 표시용 라벨이다. 닉네임으로 참여자를 조회·매칭하는 코드를 쓰지 않는다.
- 재접속은 `studentSessionId`로 복구한다. **닉네임만으로 기존 참여자에 자동 연결하지 않는다.**
- **교사용 파일 업로드 기능을 만들지 않는다.** BGM·효과음·이미지는 모두 관리자·개발자가 등록한 자산에서만 선택한다. 업로드 UI, 스토리지 쓰기 권한, 파일 검증 로직을 추가하지 않는다.

---

## 5. 구현 후

### 5.1 기능을 구현하면 테스트를 쓴다

- 기능 구현과 테스트 작성은 한 세트다. "나중에 테스트"는 하지 않는다.
- **Vitest** — 채점 로직, 힌트 해제 판정, 출구 개방 조건, 팀 균등 배정, 좌표 클램프, 게시 검증, 진행 상태 갱신 등 순수 로직.
- **Playwright** — 교사 제작→게시, 학생 접속→플레이→탈출, 재접속 복구, 도움 요청→교사 확인 완료 등 주요 흐름.
- 아래는 **반드시** 테스트를 만든다.
  - 정답이 학생용 API 응답과 페이지 HTML에 포함되지 않는지
  - 미리보기가 학생과 동일한 서버 판정 경로를 쓰는지
  - 소유 교사가 아니면 편집 화면 정답 조회가 거부되는지
  - 해당 학생의 오답 2회 전에는 힌트가 내려오지 않는지
  - **팀전에서 팀원의 오답이 다른 팀원의 힌트를 열지 않는지**
  - **팀전에서 팀원 1명의 정답이 팀의 해당 문제를 해결 완료로 만드는지**
  - 방의 문제를 다 풀기 전에 출구가 열리지 않는지
  - 게시본이 수정되지 않고, 진행 중 세션이 시작 당시 버전을 계속 쓰는지
  - **교사 확인 완료가 정답 기록과 분리 저장되는지**
  - 닉네임이 중복돼도 진행 상태가 섞이지 않는지
  - RLS 정책이 남의 데이터 접근을 막는지
- 테스트를 통과시키려고 **테스트를 약화시키거나 단언을 지우지 않는다.** 구현을 고친다.

### 5.2 검증하지 않은 완료를 보고하지 않는다

- "됐습니다", "통과합니다"라고 말하기 전에 **실제로 테스트를 실행**한다.
- 실행하지 못했으면 "실행하지 못했다"고 그대로 말한다.
- 실패한 테스트가 있으면 **숨기지 말고 출력과 함께 보고**한다.

### 5.3 작업이 끝나면 이렇게 보고한다

매 작업 종료 시 아래를 보고한다.

1. **변경한 파일 목록** — 생성/수정/삭제 구분
2. **테스트 결과** — 실행한 명령과 통과/실패 개수. 실패가 있으면 실패 내용
3. **적용한 결정과 가정** — 사양에 없어 임의로 판단한 부분
4. **하지 않은 것** — 요청 범위 중 빠진 부분과 그 이유

보고는 짧고 사실 위주로 쓴다.

---

## 6. Git

- 사용자가 요청하지 않으면 `commit`, `push`, 브랜치 생성을 **실행하지 않는다.**
- `--no-verify`로 훅을 건너뛰지 않는다.
- 강제 푸시, `reset --hard` 같은 되돌리기 어려운 명령은 먼저 확인을 받는다.

---

## 7. 코드 스타일

- **TypeScript strict**. 새 코드에 `any`를 쓰지 않는다. 불가피하면 이유를 주석으로 남긴다.
- 스타일은 **Tailwind CSS**로 한다. 별도 CSS 파일이나 인라인 style을 기본 수단으로 쓰지 않는다.
- 서버 전용 코드(정답 판정, service key 사용)에는 서버 전용임이 드러나게 한다. 클라이언트 번들에 섞이지 않게 한다.
- 파일·폴더 이름과 코드 컨벤션은 **주변 코드를 따른다.**
- 주석은 주변 코드와 같은 밀도로 쓴다. 자명한 코드에 주석을 달지 않는다.
- 사용자 화면 문구는 한국어이며, 초등학생이 읽을 수 있는 수준으로 쓴다.
